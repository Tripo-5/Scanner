# config.py
web_config = {
    "enabled": False,  # Toggle this from the menu
    "host": "0.0.0.0",
    "port": 5000,
    "debug": True,
    "admin_user": "admin",  # Default credentials (must be changed on first login)
    "admin_pass": "changeme",
    "db_host": "localhost",
    "db_user": "scanner_user",
    "db_pass": "scanner_pass",
    "db_name": "scanner_db"
}
